import { ChevronDown, ChevronUp } from 'lucide-react'
export default function DataTable({ columns, data, sortKey, sortDir, onSort, actions }) {
  return <div className="overflow-x-auto rounded-2xl border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900"><table className="min-w-full divide-y divide-slate-200 text-sm dark:divide-slate-800"><thead className="bg-slate-50 dark:bg-slate-800"><tr>{columns.map(c => <th key={c.key} className="px-4 py-3 text-left font-bold text-slate-600 dark:text-slate-200"><button className="inline-flex items-center gap-1" onClick={() => onSort?.(c.key)}>{c.label}{sortKey === c.key && (sortDir === 'asc' ? <ChevronUp size={14} /> : <ChevronDown size={14} />)}</button></th>)}{actions && <th className="px-4 py-3 text-right">Actions</th>}</tr></thead><tbody className="divide-y divide-slate-100 dark:divide-slate-800">{data.map((row, i) => <tr key={row.id || i} className="hover:bg-primary-50/50 dark:hover:bg-slate-800/70">{columns.map(c => <td key={c.key} className="px-4 py-3 text-slate-700 dark:text-slate-200">{c.render ? c.render(row) : row[c.key]}</td>)}{actions && <td className="px-4 py-3 text-right">{actions(row)}</td>}</tr>)}</tbody></table></div>
}
