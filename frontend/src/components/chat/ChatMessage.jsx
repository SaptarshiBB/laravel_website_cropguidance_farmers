import { Bot, User } from 'lucide-react'
export default function ChatMessage({ message }) { const bot = message.sender === 'bot'; return <div className={`flex gap-3 ${bot ? '' : 'justify-end'}`}>{bot && <div className="grid h-9 w-9 place-items-center rounded-full bg-primary-100 text-primary-700"><Bot size={18}/></div>}<div className={`max-w-[80%] rounded-2xl px-4 py-3 text-sm ${bot ? 'bg-white text-slate-700 shadow-sm dark:bg-slate-800 dark:text-slate-100' : 'bg-primary-600 text-white'}`}><p>{message.text}</p><span className="mt-1 block text-[10px] opacity-70">{message.time}</span></div>{!bot && <div className="grid h-9 w-9 place-items-center rounded-full bg-slate-200 text-slate-700"><User size={18}/></div>}</div> }
