import WeatherIcon from '../ui/WeatherIcon'
export default function WeatherCard({ weather }) { return <div className="rounded-2xl bg-gradient-to-br from-sky-500 to-primary-600 p-6 text-white shadow-md"><div className="flex items-start justify-between"><div><p className="text-sm opacity-90">{weather?.city}, {weather?.state}</p><h3 className="mt-2 text-5xl font-extrabold">{weather?.temperature ?? 29}°C</h3><p className="mt-1 font-medium">{weather?.condition || 'Partly cloudy'}</p></div><WeatherIcon condition={weather?.condition} size={52}/></div><div className="mt-6 grid grid-cols-3 gap-3 text-sm"><span>Humidity {weather?.humidity ?? 64}%</span><span>Wind {weather?.wind_speed ?? 12} km/h</span><span>Rain {weather?.rainfall ?? 2} mm</span></div></div> }
