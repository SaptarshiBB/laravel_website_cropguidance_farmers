import { useState } from 'react'
import AlertBadge from '../ui/AlertBadge'
import RiskMeter from '../ui/RiskMeter'
import Button from '../ui/Button'
import { severityScore } from '../../utils/constants'
export default function PestCard({ alert }) { const [open,setOpen]=useState(false); return <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div className="flex items-start justify-between gap-4"><div><h3 className="text-lg font-extrabold">{alert.pest_name}</h3><p className="text-sm text-slate-500">Crop: {alert.crop?.name || 'Multiple crops'}</p></div><AlertBadge severity={alert.severity}/></div><div className="mt-4 flex items-center justify-between"><div className="text-sm"><b>States:</b> {(alert.affected_states || []).join(', ')}<p className="mt-2 text-slate-600 dark:text-slate-300">{alert.symptoms}</p></div><RiskMeter value={severityScore[alert.severity] || 30}/></div><button onClick={() => setOpen(!open)} className="mt-4 text-sm font-bold text-primary-700">{open ? 'Hide prevention' : 'Show prevention'}</button>{open && <p className="mt-3 rounded-xl bg-primary-50 p-3 text-sm dark:bg-primary-900/30">{alert.prevention}</p>}<Button className="mt-4 w-full" variant={alert.severity === 'critical' ? 'danger' : 'secondary'}>{alert.emergency_action || 'Contact agriculture officer'}</Button></div> }
